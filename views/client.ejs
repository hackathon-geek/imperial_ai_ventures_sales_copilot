<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Video Chat</title>
    <script>
        const ROOM_ID = "<%= roomId %>";
        // console.log(`ROOM_ID = ${ROOM_ID}`)
    </script>
    <script defer src="https://unpkg.com/peerjs@1.5.4/dist/peerjs.min.js"></script>
    <script defer src="/socket.io/socket.io.js"></script>
    <script defer src="keys.js"></script>
    <script defer src="prompt.js"></script>
    <script defer src="script-client.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        :root {
            --primary-color: #2ecc71;
            --secondary-color: #3498db;
            --accent-color: #f39c12;
            --background-color: #f0f3f5;
            --text-color: #2c3e50;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--background-color), #d6e4ff);
            color: var(--text-color);
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        header {
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            color: white;
            text-align: center;
            padding: 1.5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            margin: 0;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        main {
            flex-grow: 1;
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 2rem;
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
            width: 100%;
        }

        .video-container {
            background: linear-gradient(45deg, #2c3e50, #34495e);
            border-radius: 20px;
            overflow: hidden;
            aspect-ratio: 16 / 9;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-size: 1.5rem;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .video-container:hover {
            transform: scale(1.02);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
        }

        .video-container.large {
            font-size: 2rem;
        }

        footer {
            background: linear-gradient(90deg, var(--secondary-color), var(--primary-color));
            color: white;
            text-align: center;
            padding: 1rem;
            margin-top: auto;
            box-shadow: 0 -4px 6px rgba(0, 0, 0, 0.1);
        }

        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }

        .float {
            animation: float 4s ease-in-out infinite;
        }
    </style>
</head>
<body>
    <header>
        <h1>Client Video Chat</h1>
    </header>
    <main>
        <div id="client-video" class="video-container float">
            
        </div>
        <div id="freelancer-video" class="video-container large float" style="animation-delay: 2s;">
            
        </div>
    </main>
    <footer>
        <p>&copy; 2025 Freelancer-Client Video Chat Platform</p>
    </footer>
</body>
</html>